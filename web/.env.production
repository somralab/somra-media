# Production placeholder. In the single-image Docker build the SPA is served
# by the Go binary so it lives on the same origin as the API. Override only
# when the SPA is hosted separately from the backend.
VITE_API_BASE_URL=/api/v1
